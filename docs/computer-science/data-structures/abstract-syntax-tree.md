# Abstract Syntax Trees

An Abstract Syntax Tree (AST) is a tree-based data structure used to represent code. Each node in an AST denotes a construct occurring in the code. The tree is 'abstract' because it
