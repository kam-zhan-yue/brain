Constants that need to be accessed throughout the app are stored in `settings/`
- `settings/base` holds stuff for the main application
- `settings/{other}` holds stuff for other applications

This allows us to use different functions and settings in different environments with the same setting configuration.