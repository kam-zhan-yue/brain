A different light colour greatly influences the object's colour output. Since the light colour directly influences what colours the object can reflect, it has a significant impact on the visual output.

We can easily change the light's colours overtime by changing the light's ambient and diffuse colours with `sin` and `glfwGetTime`