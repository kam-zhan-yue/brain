The structure of a function in bash is similar to most languages.

```shell
function function_name() {
	<commands>
}
```

You can also omit the `function` keyword.

```shell
hello() {
	echo "Hello World"
}

hello
```

