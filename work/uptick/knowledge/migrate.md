When you want to target a specific workforce, do
```shell
migrate --help
```

Which will bring up a list of workspaces. An example of such a command is:

```shell
migrate --workspaces 'frontend/general-components/component-library'
```