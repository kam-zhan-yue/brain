## Tasks
- [x] Morning off
- [x] Follow up with TSR Search Params
- [x] Follow up with TSR Dev Mode
- [x] Follow up with Dispatch PR
- [x] Generate Routes for Chengyin
- [x] Routine Editor Refactor - Start Backend
- [x] Checkout TSR Search Params

### Routine Editor Create

This create form is a bunch of nested forms.

Essentially, we need to create a bunch of routine services with the following information:

```
payload: {
	annualDate
	startDate
	endDate
	subcontractor
	
	routineServices: [
		levels: []
		overrides: {
			annualDate,
			startDate,
			endDate,
			contractor
		}
	]
}
```

Or can we just bulk create them in the first place? It's kind of a customised font, but the form comes with validation that we can just do, right? Yes.

Right now, when the requests are complete, we will return all of the routine services. This avoids having to fetch again. However, I don't think that is possible in our case. We might want to fetch again when opening that modal. The onSuccess should refetch!


OMg we need so many unit tests now...
I need to fully understand how the routine creation works in the backend

## TSR Search Param

The problem code is in `useTaskProfitabilityState`. It will return a `useSearchParamsState` which will create search params of sections.

`parse` will call `Number.parseInt(params.actvity_range)`

`use-search-params-state` line 71 has

```tsx
let state = section.parse(curParams)
```


When analysing curParams,
```
React Router
{
  "activity_range": "0",
  "is_active": "true",
  "branch": "",
  "category": ""
}
```

```
Tanstack Router
{
  "activity_range": "0",
  "is_active": "true",
  "branch": "",
  "category": ""
}
```

However, when refreshing, it renders twice

```
{
  "activity_range": "\\0\\",
  "is_active": "\\true\\",
  "branch": "",
  "category": ""
}
{
  "is_active": "\\true\\",
  "branch": "",
  "category": ""
}
```