## Tasks
- [x] Fix up PRs
- [ ] Acceptance Tests?
- [ ] Continue with Routing

### Unbarelling

In order to unbarrel effectively, we need to
- Store the `tsconfig` into a dictionary by name. E.g. `@uptick/component-library`: `tsconfig`
- Get out the `exports` of the `tsconfig` and resolve it
- Go to that file

Alternatively, we can just expand out the wildcard exports for now, then unbarrel later