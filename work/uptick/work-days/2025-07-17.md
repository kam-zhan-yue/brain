## Tasks
- [x] Fix Routing PR
- [x] Sentry Duty
- [x] Review PRs
- [x] Bank Appointment
- [x] Frontend Hackathon

## Frontend Fatal Errors

### Type Errors
- `o.findLastIndex`
- `Cannot read properties of null`
- `Cannot read properties of undefined`
- `rf(...).toSorted` is not a function
- Cannot user `in` operator to search for

## Api List

Select all is causing the selection to return 'all'

ApiList.tsx line 1678
```typescript
<a className="btn btn-link btn-sm" href="#" onClick={this.handleSelectAllClick}>
Select all {count.toLocaleString()} items
</a>
```

APIList is a wrapper over `ObjectList`

APIList.selectItems -> calls `this.setState({ selection: 'all' })`

Ok so now how is `selectedSessions` being set to 'all'?