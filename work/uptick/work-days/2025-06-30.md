## Tasks
- [x] Figure out authentication with TanStack Router
- [x] Refactor Dispatches
- [x] Refactor Defect Quotes

### TanStack Router Auth

* When going into a route when not authenticated, it should look like
```typescript
export const Route = createFileRoute('/dispatches/*')({
  component: () => {
    const isAuthed = !!useAuth()?.user
    if (!isAuthed)
	    return <Login />
    return <Dispatches />
  },
})
```

Then, in `Dispatches`, we could get rid of the `NotFoundPage` route since TanStack will handle that for us!

We're gonna need the following:
- Hard coded auth route. If auth is required, then we add the following to the file

```typescript
  component: () => {
    const isAuthed = !!useAuth()?.user
    if (!isAuthed)
	    return <Login />
    return <Component />
  },
```

## Broken Environment
- [ ] inv docker.nuke
- [ ] bun upgrade
- [ ] mise run:clean:frontend
- [ ] mise run reset
- [ ] make space
- [ ] mise run reset:frontend (same as bun reset)