## Tasks
---
- [x] Standup
- [x] Chat with Kevin on Tanstack Query
- [x] Review Alex's PR
- [x] Review Jasper's PR
- [x] Shower and Eat Breakfast
- [x] Fix up Schedule Tab PRs
- [x] Chat with Ryan on React Query
- [x] Check Favourite Filters with Tanstack
	- [x] The search param is being shown as "{search} " instead of "{search}%20"
	- [x] When refreshing, the search param is also reset
- [x] Check Failing Frontend Routing Issue
- [x] Fix up Routines PRs

### Schedule Tab
Overall section updates  
- [x] Can we remove all padding from h6 headings (‘appointments’ & ‘access protocol’)
- [x] Update overall card body gap spacing to `gap-3`

Appointment section  
- [x] Can you remove the background/padding from the appointment section. So remove the following classes `p-2 bg-light-subtle rounded-2 border border-1 border-light` from the parent div
- [x] For _past appointments_, can you update the repeating-linear-gradient line colour to be `rgba(0, 0, 0, 0.05)` 
- [x] For the both appointment sections (upcoming and past)
    - [x] can we update the gaps so the space between upcoming and past is `gap-2` (currently gap-1) (image 2)
    - [x] Can we make the gap between appointment rows within each of those sections `gap-1` (currently gap-2) (image 3 & 4)

Access Protocol  
- [x] Can you remove the bottom margin from the alert section, it’s adding more space between that section and the buttons below it
- [x] Can you make all instances of DT’s ‘font-weight 500

### Tanstack Query Follow Ups
- Chat with Ryan on whether his stuff on schema generation will affect us. Namely, do we need to change things on our end?
- The final task redesign is having a three-column layout with a persistent right column. The right column shouldn't be refetching anything. 
