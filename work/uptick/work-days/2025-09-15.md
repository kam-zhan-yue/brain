## Tasks
- [x] Tanstack Optimisation
- [x] Talk to Marc on Unbilled Amount
- [x] Merge Dispatch Unit Tests

### Tanstack Optimisation

There are a few problems here
1. `useTanstackResolvePath` is constantly re-rendering on location change. If we were to remove this, we could prevent re-rendering from occurring.
2. `useMatch` sucks
3. Links are still re-rendering

## Vanilla Stuff
Renders:
1. `CustomerPortalMatch` -> Match Router State
2. Link Sync External Store
3. `CustomerPortalMatch` -> Match Router State
4. The actual render

We can fix the customer portal matches if we use the react router match for react router and params for tanstack router

### Unbilled Amount

When cemented profitability is complete, we can replace the generate profitability query in the get unbilled amount. However, in the model's `get_unbilled_amount`, we need to do a logical comparison against `get_unbilled_amount` and the profitability glossary to find if they are doing anything different.