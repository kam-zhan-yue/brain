## Tasks

Weekend
- [x] Implement Remaining Rogue Translations (Labour, Cost Centre) (1 hour)
- [x] Fix bugs on @uptick/navigation (1 hour)
- [x] Fix typing issues on @uptick/navigation (1 hour)

Monday
- [x] Figure out `isAuth`
- It is passed into as `contextApi?.context?.user.id`
- I reckon we just have an `_auth-routes` pathless layout
- [ ] Refactor Scheduler Routes
- [ ] Update the route generation algorithm (want to generate for a specific tree)

## Remaining Translations
- [x] Control Panel Labour Rate (Page Proxied)
- [x] Task Profitability Columns (Not Needed)
## Broken Pages

### Scheduler
- [x] Clicking on an appointment doesn't do anything
- It seems like the `onClick` event is propagated all the way from `TaskScheduler` in `handleClickEvent`
- Holy shit it's a combination of URLSearchParams and `navigate`

### NavLink
- [x] `tab-link.tsx` is using `NavLink` from react-router-dom

### Conditional Link
- [x] Conditional Link is passing through `to={to as To}`

### RouterLink
- [x] There are many places in the codebase where we do `import { Link as RouterLink }`