## Tasks
- [x] Merge TaskV2Context Removal
- [x] Merge Standardise PDF Button
- [x] Consolidate feature flag logic
- [x] Generate Routes CI Check (unit test or own test?)
- [x] Look into Allow Stock to be Selected Before moving off task
- [x] Look into why tanstack router is allowing string in paths -> submitted question
- [x] Reviews
- [x] ESBuild

## Task Stock Modal
- [x] Fix hook needs to be refetched
Currently, we store stock transfers in TaskStockFifoTransaction. However, if there are transactions of the same product, but are in different transactions, then they are separate. 

In the UI, we show this as combined. E.g. if there are two transaction of Product A, one at 20 and one at 56, then the UI shows 76. However, if we want to select a number to transfer, which one do we take from? 

Presumably the one that was created earliest? Needs more discussion with an SME. For now, back to the drawing board
## Work

ES Build Bundle Size Analyser
- Check ESBuild workflow
- See how to use the analyser
- Send to metrics
