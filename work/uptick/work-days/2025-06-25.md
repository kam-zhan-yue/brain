## Tasks
- [x] Wrap up Routing PRs
- [x] Wrap up Logbooks Bug
- [x] Check out Mark a task as complete, but not inactive during dispatching
- [x] Merge PDFs in Dispatch AT

## Merge PDFs AT

**Merge All PDFs**
- Merges all reports, form responses, joyfill form responses, defect quotes, and invoice
- Outputs only one final PDF
**Merge Reports**
- Merges all reports
- Merges all defect quotes
- Merges all form responses
- Merges all joy fill form response

> Both tests do not merge the file attachments (because they might not be PDFs)

### Attach Document to Dispatch

Option 1: Mock `MediaStorage`
- Attachments are added in `InvoiceEmail._add_attachments`
- This uses the file path directly provided in the path of `file_attachments`
- `default_storage` will call `S3Boto3Storage._open`

```python
def _open(self, name, mode='rb'):
	name = self._normalize_name(clean_name(name))
	try:
		f = S3Boto3StorageFile(name, mode, self)
	except ClientError as err:
		if err.response['ResponseMetadata']['HTTPStatusCode'] == 404:
			raise FileNotFoundError('File does not exist: %s' % name)
		raise  # Let it bubble up if it was some other error
	return f
```

- Probably best to just Mock something entirely and return a random file lol

Option 2: Send to a `https://pdf.com` echo
- Because the code below just checks the file like this, might be best to echo it to an API
- Might have to check if we can still link to task though
```python
if any(attachment["path"].startswith(p) for p in ["https://", "http://"]):
	try:
		f = request.urlopen(attachment["path"])  # noqa: S310
```

## Scheduler Bugs

### Issue 1: Clicking on Task ID not Opening Model
- Seems to be happening with scheduled tasks only.
- [See loom here](https://www.loom.com/share/a1a18cc0cd664f98ac5fa8be9ca1e0bd?sid=86e778c4-2dd8-45bc-98a0-b360d66f8c3c)

### Issue 2: Work Tab not working on Tasks


### Issue 3: Scrolling down keeps going to blank screen
- [See loom here](https://www.loom.com/share/0976b7f876164fd780256fcdef1fa170?sid=4d24e2e4-2bd7-41ba-8f12-0fbf37a664bd)