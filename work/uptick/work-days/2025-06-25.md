## Tasks
- [ ] Wrap up Routing PRs
- [ ] Wrap up Logbooks Bug
- [ ] Attach Document to Dispatch AT
- [ ] Merge PDFs in Dispatch AT

### Attach Document to Dispatch

Option 1: Mock `MediaStorage`
- Attachments are added in `InvoiceEmail._add_attachments`
- This uses the file path directly provided in the path of `file_attachments`
- `default_storage` will call `S3Boto3Storage._open`

```python
def _open(self, name, mode='rb'):
	name = self._normalize_name(clean_name(name))
	try:
		f = S3Boto3StorageFile(name, mode, self)
	except ClientError as err:
		if err.response['ResponseMetadata']['HTTPStatusCode'] == 404:
			raise FileNotFoundError('File does not exist: %s' % name)
		raise  # Let it bubble up if it was some other error
	return f
```

- Probably best to just Mock something entirely and return a random file lol

Option 2: Send to a `https://pdf.com` echo
- Because the code below just checks the file like this, might be best to echo it to an API
- Might have to check if we can still link to task though
```python
if any(attachment["path"].startswith(p) for p in ["https://", "http://"]):
	try:
		f = request.urlopen(attachment["path"])  # noqa: S310
```